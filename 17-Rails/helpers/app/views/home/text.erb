<h1>This is about helpers for text!</h1>

<h3>Pluralize helper uses pluralize(@variable_name, 'singular_item')</h3>
<p>There are <%= pluralize(@person_count1, 'person') %> signed up for office hours</p>
<p>There are <%= pluralize(@person_count2, 'person') %></p>

<br> <!-- Line break -->

<h3>No truncate: <%= @story %> </h3>
<h3>Truncate: <%= truncate(@story, :length => 20) %></h3>

<br>

<ul>
	<%= @numbers.each do |num| %>
	<li style="background-color:<%= cycle('red', 'blue', 'green', 'orange', 'yellow', 'purple') %>;">
	<%= num %> </li>
	<% end %>
	</ul>
</ul>

